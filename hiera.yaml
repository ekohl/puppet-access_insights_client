---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Operating System Release"
    path: "%{facts.os.family}_%{facts.operatingsystemrelease}.yaml"
  - name: "common"
    path: "common.yaml"
